import { BlockAdminClient } from "./BlockAdminClient";

export const metadata = {
  title: "Block Admin | Editor Visual",
  description: "Editor visual de bloques para landing pages",
};

export default function BlockAdminPage() {
  return <BlockAdminClient />;
}
